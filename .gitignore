# Local .terraform directories
**/.terraform/

# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Terraform variable files (often contain secrets)
*.tfvars
*.tfvars.json

# Override files (custom configuration not intended for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plan output (can be large or contain sensitive info)
*.tfplan

# .terraform.lock.hcl can be committed (optional: remove if using multiple providers/versions)
# Uncomment the next line to ignore it:
# .terraform.lock.hcl

# Terraform provider logs (if enabled via TF_LOG)
terraform.log

# IDE/editor specific files
.vscode/
.idea/
*.swp
*.bak
*~

# OS-specific
.DS_Store
Thumbs.db
